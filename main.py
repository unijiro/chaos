def main():
    print("Hello from chaos!")


if __name__ == "__main__":
    main()

'''
How can I upload this project to remote git repository?To upload your project to a remote Git repository, follow these steps:
1. Initialize a Git repository (if you haven't already):
   ```
   git init
   ```
2. Add your files to the staging area:
   ```
   git add .
   ```
3. Commit your changes:
   ```
   git commit -m "Initial commit"
   ```
4. Add a remote repository (replace <REMOTE_URL> with your repository URL):
   ```
   git remote add origin <REMOTE_URL>
   ```
5. Push your changes to the remote repository:
   ```
   git push -u origin master
   ```

'''